# 1 Verificação de Aprendizagem

**INSTRUÇÕES**
- A prova é individual e sem consulta a colegas.
- Durante a prova, é permitido o acesso às seguintes fontes:
    - Documentação oficial do React Native: [https://reactnative.dev/docs](https://reactnative.dev/docs)
    - Documentação oficial do Expo: [https://docs.expo.dev](https://docs.expo.dev)
    - Documentação oficial do TypeScript: [https://www.typescriptlang.org/docs](https://www.typescriptlang.org/docs)
    - Documentação oficial do GitHub: [https://docs.github.com](https://docs.github.com)
    - Anotações de aula (Google Classroom ou Notion da disciplina)
- Alunos que, durante a prova, estejam usando pesquisa do Google (modo AI) ou ChatGPT, Gemini, Perplexity ou qualquer outra LLM, terá a sua avaliação ANULADA!
- **Cada quesito é independente** — a resolução de um não depende do anterior.
- A entrega deve ser feita por meio do **Google Classroom na Atividade 1 VERIFICAÇÃO DE APRENDIZAGEM**.
- Submeta o seu projeto compactado em arquivo ZIP (não inclua no ZIP a pasta **node_modules** !)
- Nenhuma solução que não compile e execute corretamente receberá pontuação plena.
- Tempo de prova: **120 minutos**.

**BOA PROVA!**

---

### O projeto base

- Realize o clone o repositório de partida disponível em: https://github.com/renanalencar/habit-tracker-expo
- Familiarize-se com a estrutura antes de iniciar:

```plain

habit-tracker-expo/

├── App.tsx
├── src/
│   ├── components/
│   │   ├── HabitItem.tsx
│   │   └── HabitList.tsx
│   ├── types/
│   │   └── Habit.ts
│   └── utils/
│       └── handle-api.ts
├── assets/
├── app.json
└── package.json

```

**Observação:** O app consome uma API REST local de hábitos. O backend não fará parte da avaliação — utilize os dados mockados já presentes no projeto caso o servidor não esteja disponível.

---

### Questão 1
(1,0 pt) O app atualmente pode apresentar problemas de sobreposição de conteúdo com a barra de status em dispositivos móveis. **Faça**:

a) Verifique se o `SafeAreaView` está configurado corretamente em` App.tsx` para que o conteúdo não fique oculto sob a status bar de dispositivos móveis. Ajuste o `paddingTop` utilizando `Platform.OS` e `StatusBar.currentHeight` quando necessário.

b) Crie um arquivo `src/styles/global.ts` com um objeto de estilos globais contendo, no mínimo:

- Uma cor primária (primaryColor) — sugerido: #5C6BC0 (índigo)
- Uma cor de fundo (backgroundColor) — sugerido: #F5F5F5
- Um tamanho de fonte padrão para corpo de texto (bodyFontSize) — sugerido: 16

c) Aplique ao menos dois dos estilos globais definidos em global.ts no componente `App.tsx`.

- **Commit**: [feat(Q1): add assessment task documentation and integrate global styles into App component](https://github.com/renanalencar/habit-tracker-expo/commit/443ef642282fce44b8884436cf1eea47031a1f52) 
- **Resultado** esperado (abaixo):

<img src="../images/Habit-Tracker-2026-04-20_16_16.png" alt="Descrição" height="500" />


---

### Questão 2
(1,0 pt) **Faça** o que é pedido abaixo:

a) No componente `HabitItem.tsx`, adicione a exibição da frequência do hábito (`frequency`: `'diário'`, `'semanal'` ou `'mensal'`). A frequência deve ser exibida como uma etiqueta (Text dentro de um View) com cores distintas para cada valor:

- `'diário'`: fundo verde claro (#C8E6C9), texto verde escuro (#2E7D32)
- `'semanal'`: fundo azul claro (#BBDEFB), texto azul escuro (#1565C0)
- `'mensal'`: fundo laranja claro (#FFE0B2), texto laranja escuro (#E65100)

b) No `App.tsx`, exiba um componente `Image` com a logo do app (já presente em `assets/habit-tracker-banner.png`). Se a imagem não carregar, exiba um componente `Text` com o texto `"Rastreador de Hábitos"` no lugar (renderização condicional com estado booleano `logoError`).

c) Adicione um `ActivityIndicator` que seja exibido enquanto os hábitos estão sendo carregados (utilize o estado loading já presente no projeto).

- **Commit**: [feat(Q2): implement HabitItem component and add conditional banner rendering in App.tsx](https://github.com/renanalencar/habit-tracker-expo/commit/8172c861116ed9358a862449315d37db3e9ee364)
- **Resultado** esperado (abaixo):

<img src="../images/Habit-Tracker-2026-04-20_16_39.png" alt="Descrição" height="500" />


---

### Questão 3
(1,5 pt) **Faça** o que é pedido abaixo:

a) O componente `HabitList.tsx` atualmente utiliza um `FlatList` simples. Substitua-o por um `SectionList` que agrupe os hábitos em duas seções com base no campo `completedToday` (booleano que indica se o hábito foi concluído hoje):

- `"Concluídos hoje"` — hábitos onde `completedToday === true`
- `"Pendentes"` — hábitos onde `completedToday === false`

b) O `renderSectionHeader` deve exibir o título da seção com fundo cinza claro (#EEEEEE), fonte em negrito (`fontWeight: 'bold'`) e padding adequado.

c) Se uma seção estiver vazia, exiba um `Text` com a mensagem `"Nenhum hábito nesta categoria."` no lugar da lista.

d) Mantenha a compatibilidade com as props já existentes: habits, `onToggle` e `onDelete`.

- **Commit**: [feat(Q3): implement HabitList component using SectionList to group habits by completion status](https://github.com/renanalencar/habit-tracker-expo/commit/143258a0a570a2386037b1c77f77720a247be9bf)
- **Resultado** esperado (abaixo):

<img src="../images/Habit-Tracker-2026-04-20_16_41.png" alt="Descrição" height="500" />


---

### Questão 4
(1,0 pt) **Faça** o que se pede abaixo:

a) No `App.tsx`, adicione um novo `useState` chamado filter do tipo `'all' | 'completed' | 'pending'` com valor inicial `'all'`. Implemente a lógica de filtragem dos hábitos:

- `'all'` → exibe todos os hábitos
- `'completed'` → exibe apenas hábitos com `completedToday === true`
- `'pending'` → exibe apenas hábitos com `completedToday === false`

b) Crie três botões de filtro (usando `TouchableOpacity` ou `Pressable`) exibidos horizontalmente abaixo do cabeçalho, com os rótulos `"Todos"`, `"Concluídos"` e `"Pendentes"`. O botão do filtro ativo deve ter fundo índigo (#5C6BC0) e texto branco; os inativos devem ter borda índigo e texto índigo.

c) O Modal de criação de hábitos já está implementado. Adicione a ele um seletor de frequência, representado por três botões (`TouchableOpacity`) com os valores `"Diário"`, `"Semanal"` e `"Mensal"`. A opção selecionada deve ser destacada visualmente com fundo colorido. Gerencie o estado com `useState<'Diário' | 'Semanal' | 'Mensal'>` e inclua o valor no objeto enviado ao salvar o hábito.

- Commit: [feat(Q4): add habit creation modal and filtering functionality to habit list](https://github.com/renanalencar/habit-tracker-expo/commit/e6cdbf60159d2c3e11a2bab89c85b7475e3360f6)
- **Resultado** esperado para o botão de adicionar novo hábito (abaixo):

<img src="../images/Habit-Tracker-2026-04-20_16_47.png" alt="Descrição" height="500" />

- **Resultado** esperado para o modal (abaixo):

<img src="../images/Habit-Tracker-2026-04-20_16_48.png" alt="Descrição" height="500" />


---

### Questão 5 
(1,5 pt) Faça o que é pedido abaixo:

a) Crie um novo componente `src/components/StatsScreen.tsx`. Esse componente deve:

- Utilizar `ScrollView` para envolver todo o conteúdo
- Exibir o título `"Minhas Estatísticas"`, um resumo textual com pelo menos três métricas simuladas (ex.: total de hábitos, hábitos concluídos hoje, sequência atual em dias) e uma lista das categorias de frequência (Diário, Semanal, Mensal) com um contador fictício para cada
- Utilizar os componentes `Text`, `View` e `Image` (pode usar a logo do projeto ou qualquer imagem em `assets/`)

b) Adicione um `Button` nativo (componente `Button` do React Native) no `App.tsx` com o título `"Ver Estatísticas"` que, ao ser pressionado, torne o `StatsScreen` visível via um Modal (`animationType: "slide"`, vindo de baixo).

c) Dentro do `StatsScreen`, adicione um `TouchableOpacity` com o texto `"Fechar"` que fecha o Modal.

d) Estilize o `StatsScreen` com um `StyleSheet` próprio contendo no mínimo: padding adequado, títulos com `fontWeight: 'bold'`, separação visual entre as seções de métricas, e uso de pelo menos uma cor de destaque.

- Commit: [feat(Q5): add StatsScreen component and integrate modal for habit statistics view](https://github.com/renanalencar/habit-tracker-expo/commit/3a4382df0097efc73315186feb0ccd04fb2ce864)
- **Resultado** esperado para o botão "Ver Estatísticas" (abaixo):

<img src="../images/Habit-Tracker-2026-04-20_16_50.png" alt="Descrição" height="500" />

- **Resultado** esperado para a tela de estatísticas (abaixo):

<img src="../images/Habit-Tracker-2026-04-20_16_51.png" alt="Descrição" height="500" />


---

## Resultado Final

![alt text](../images/Habit-Tracker.gif)